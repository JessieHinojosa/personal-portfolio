import React from 'react'

const MdButton = (props) => {
    return (
        <div className={props.buttonClass}
        
        >
            {props.buttonText}
        </div>
    )
}

export default MdButton
